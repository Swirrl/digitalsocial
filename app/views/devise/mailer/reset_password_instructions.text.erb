Hello <%= @resource.first_name %>,

Someone has requested to change your password on digitalsocial.eu

You can do this through the link below:

<%= edit_password_url(@resource, :reset_password_token => @resource.reset_password_token) %>

If you didn't request this, please ignore this email.

<%= render "request_mailer/footer" %>

