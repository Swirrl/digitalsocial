%table.index
  %tr
    %th Blog post
    %th.center Status
    %th.center Publish at
    %th.center Author
    %th

  - @blog_posts.all.each do |blog_post|
    %tr
      %td= blog_post.name
      %td.center= blog_post.status.titleize
      %td.center= blog_post.publish_at.strftime("%-d %B %Y")
      %td.center= blog_post.admin.name
      %td.actions
        = link_to "Edit", [:edit, :admin, blog_post]
        = link_to "Delete", [:admin, blog_post], method: :delete, confirm: "Are you sure you wish to delete this blog post?"

= link_to "New blog post", [:new, :admin, :blog_post]