%table.index
  %tr
    %th.center Page
    %th.left Body
    %th

  - Page.all.each do |page|
    %tr
      %td.center= page.name.titleize
      %td.left= page.body.truncate(80)
      %td.actions
        = link_to "Edit", [:edit, :admin, page]
        =# link_to "Delete", [:admin, page], method: :delete