.dashboard.projects

  - if current_organisation
    = render "dashboard/tabs", active: "Activities"

    .title
      %h2
        %strong #{current_organisation.name}'s
        Activities

    = render "dashboard/projects/list"