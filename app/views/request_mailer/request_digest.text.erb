Hi <%= @user.first_name %>,

<% if @organisation.pending_project_invites.any? %>
<%= @organisation.name %> has pending invites to the following activities:

<% @organisation.pending_project_invites.each do |inv| %>
* <%= inv.project_resource.name %>
<% end %>
<% end %>

<% if @organisation.pending_project_requests_by_others.any? %>
The following organisations have requested to join <%= @organisation.name %>'s activities:

<% @organisation.current_organisation.pending_project_requests_by_others.each do |req| %>
* <%= req.requestor_organisation_resource.name %> (<%= req.project.name %>)
<% end %>
<% end %>

<% if @organisation.has_respondable_user_requests? %>
The following users have requested to join your organisation:

<% @organisation.respondable_user_requests.each do |request| %>
* <%= request.user_first_name %> (<%= request.user_email %>).requestor_organisation_resource.name
<% end %>
<% end %>

To respond to these items, please log in to the digitalsocial.eu dashboard for <%= @organisation.name %> (link below).

<%= user_uri(org_id: @organisation.guid) %>