Hi <%= @user.first_name %>,

<% if @organisation.pending_project_invites.any? %>
<%= @organisation.name %> has pending project invites to the following projects:

<% @organisation.pending_project_invites.each do |inv| %>
* <%= inv.project_resource.name %>
<% end %>
<% end %>

<% if @organisation.pending_project_requests_by_others.any? %>
<%= @organisation.name %> has pending project requests from the following organisations:

<% @organisation.current_organisation.pending_project_requests_by_others.each do |req| %>
* <%= req.requestor_organisation_resource.name %> (<%= req.project_resource.name %>)
<% end %>
<% end %>

<% if @organisation.pending_project_invites.any? ||
  @organisation.pending_project_requests_by_others.any? %>

Please log in to your DSI dashboard (link below) to respond to these project requests:

<%= projects_url(auth_token: @user.authentication_token, org_id: @organisation.guid) %>

<% end %>
<% if @organisation.has_respondable_user_requests? %>
You have pending users to invite to your organisation:

<% @organisation.respondable_user_requests.each do |request| %>
* <%= request.user_first_name %> (<%= request.user_email %>).requestor_organisation_resource.name
<% end %>

<% end %>