Hello <%= @user.first_name %>,

You have been invited to join a project on DSI.

Click the link below to sign up and accept the invitation:

TODO: Link to log in and change password etc.
<%# edit_invited_user_url(
   @user,
   auth_token: @user.authentication_token,
   org_id: @invite.invited_organisation.guid) %>