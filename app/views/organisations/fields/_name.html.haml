.field-with-notes
  .fields
    - if f.object.persisted?
      = f.input :name, label: 'Name of your organisation', input_html: { autocomplete: :off }

    - else
      = f.input :name, label: 'Name of your organisation', input_html: { class: 'organisation-auto-suggest', autocomplete: :off }
      
  .notes
    %p We are interested in hearing from both formal and informal groups.

= render 'suggestions', button_text: 'Request to join', action_url_template: '/organisations/:organisation_id/request_to_join', help: "The following organisations matched that name. If your organisation already exists click <strong>Request to join</strong> to send a request to the organisation's members."