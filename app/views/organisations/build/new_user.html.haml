.wizard
  = render "breadcrumbs", active_index: 0

  .wizard-intro
    %h3 Create an account

    %p
      Please create an account to continue. If you already have an account, you can #{link_to "log in", new_user_session_path}.

  = simple_form_for @user, url: [:organisations, :build, :create_user] do |f|

    .field-with-notes
      .field
        = f.input :first_name, placeholder: 'Enter your name', label: 'Name'
      .notes
        %p So we know how to address you in emails etc.
    .field-with-notes
      .field
        = f.input :email, placeholder: 'Enter your email address'
      .notes
        %p You'll use this to log in.
    .field-with-notes
      .field
        = f.input :password, placeholder: 'Enter a new password', required: true
      .notes



    .wizard-button-wrapper
      = f.button :submit, 'Next step &raquo;'.html_safe, class: 'button-green'
